# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# Fixtures for User records. Ensure required non-null columns are present.
one:
  email: test_one@test.com.ph
  username: test_one
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

two:
  email: test_two@test.com.ph
  username: test_two
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
